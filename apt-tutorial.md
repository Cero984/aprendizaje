# APT Tutorial

APT is a package manager. These are its most common uses:

sudo apt update: Used to check for available updates in the system.
sudo apt install: Used to install programs and apps from the terminal.
sudo apt remove: Used to remove programs and apps from the terminal.
sudo apt upgrade: Used to install all available updates. Updates the whole system.
